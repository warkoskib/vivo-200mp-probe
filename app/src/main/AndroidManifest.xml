<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />

    <queries>
        <package android:name="com.android.camera" />
        <package android:name="com.vivo.camera2" />
        <package android:name="com.vivo.camera2pd" />
        <package android:name="com.vivo.engineercamera" />
        <package android:name="com.vivo.alphacamera" />
        <package android:name="test.com.vivo.yzz.cameratestforapi" />
    </queries>

    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vivo200MPProbe"
        android:label="Vivo Camera Probe">

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>
